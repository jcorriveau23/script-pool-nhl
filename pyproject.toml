[project]
name = "nhl_helper"
version = "0.1.0"
description = "helper to fetch nhl api results and store it in database"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.13.4",
    "lxml>=6.0.1",
    "mypy>=1.17.1",
    "pydantic>=2.11.7",
    "pymongo>=4.14.1",
    "requests>=2.32.5",
    "ruff>=0.12.10",
    "schedule>=1.2.2",
]
